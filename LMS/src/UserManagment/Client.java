package UserManagment;

public class Client extends User {

    public Client(int userId, String user, String password, String phoneNumber) {
        super(userId, user, password, phoneNumber);
    }
}
